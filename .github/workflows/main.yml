name: Node CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Install
      uses: mkramb/yarn-action@v1
      with:
        args: 'install'

    - name: Hello
      uses: mkramb/yarn-action@v1
      with:
        args: 'run hello'
